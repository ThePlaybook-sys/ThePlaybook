name: Diagnostic Test

on:
  push:
    branches: [dev]
    paths:
      - ".github/workflows/diagnostic-test.yml"
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello"

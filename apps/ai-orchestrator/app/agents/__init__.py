"""Milestone 4.2 -- the shared agent output contract (Volume 4 Section
2.1) and the validation harness every future agent's output must pass
before being wired into the real pipeline (Milestone 4.4+).

No agent, no model adapter, and no persistence write exists in this
package yet -- it defines and validates the *shape* real agents will
eventually produce, nothing more.
"""

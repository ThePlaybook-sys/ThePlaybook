"""Writes normalized adapter output into Supabase (Volume 3). Raw httpx +
PostgREST, matching the pattern already established in
apps/api-gateway/app/auth.py -- no supabase-py SDK dependency anywhere in
this service.
"""

# Secrets Management

Implements Volume 2 §9 (DevOps & CI/CD) and CLAUDE.md's Credentials & Connections section.

## Where secrets live

- **Runtime secrets** (model API keys, provider keys, Twilio, Supabase service role key, internal service token) are set as Railway environment variables, scoped per environment (`dev`, `staging`, `production`). Each environment gets its own values — a `dev` key is never reused in `production`.
- **CI/CD-time secrets** (`RAILWAY_TOKEN`, used by `.github/workflows/ci-cd.yml` to deploy) are set as GitHub Actions secrets, scoped per GitHub Environment (`dev`, `staging`, `production`) matching the branch-to-environment mapping in Volume 2 §9.
- Nothing in either category is ever committed to the repo. `.env` and `.env.local` are gitignored at the repo root for local development use only, and local `.env` values are never the source of truth for a deployed environment — Railway's environment variables are.

## Who can create/rotate what

Per CLAUDE.md: API keys and provider credentials cannot be generated by Claude Code. Mac creates each one on the provider's own dashboard, then provides it once so it can be set as a Railway/GitHub environment variable — never hardcoded, never echoed back in full after being set.

## Current secret inventory

| Secret | Category | Set where | Rotation |
|---|---|---|---|
| `RAILWAY_TOKEN` | Platform (CI/CD deploy) | GitHub Actions environment secrets | On suspected compromise, or whenever Railway project access changes |
| Supabase service role key | Platform | Railway env vars (API Gateway, AI Orchestrator, Sports Intelligence Layer, Workers) | On suspected compromise |
| Supabase anon/public key | Platform | Railway env vars (Frontend), frontend build config | On suspected compromise |
| OpenAI API key | Model provider | Railway env vars (AI Orchestrator) | Quarterly (Volume 2 §9) |
| Anthropic API key | Model provider | Railway env vars (AI Orchestrator) | Quarterly (Volume 2 §9) |
| Sports/odds data provider key(s) | Data provider | Railway env vars (Sports Intelligence Layer) | Per provider's own recommended schedule; quarterly if unspecified |
| Twilio Account SID / Auth Token | Messaging | Railway env vars (API Gateway, Workers) | On suspected compromise |
| Internal service token | Internal | Generated once, set as a Railway env var on every service that calls another service internally (Volume 2 §6) | On suspected compromise |
| Sentry DSN | Observability | Railway env vars (all services) | Not sensitive enough to require rotation, but replace if the project is recreated |

Entries above with no key value yet are placeholders for what each phase will need — see `PROGRESS.md` for which ones are outstanding right now.

## Supabase project-to-environment mapping

Per Volume 3 §12, `dev`/`staging`/`production` are fully separate Supabase projects, not schemas within one — same reproducibility-protection principle as Railway's environment split. Provisioned 2026-08-07 (org `ThePlaybook-sys's Org`, Pro plan):

| Environment | Project name | Project ref | Database host | Railway env vars set |
|---|---|---|---|---|
| `dev` | `ThePlaybook-sys's Project` (pre-existing) | `nhwjtsdebgiwskshzqiq` | `db.nhwjtsdebgiwskshzqiq.supabase.co` | `SUPABASE_URL` (all 6 services), `SUPABASE_ANON_KEY` (frontend) |
| `staging` | `theplaybook-staging` | `jhpjdjtvzzmhxvprsfaq` | `db.jhpjdjtvzzmhxvprsfaq.supabase.co` | `SUPABASE_URL` (all 6 services), `SUPABASE_ANON_KEY` (frontend) |
| `production` | `theplaybook-production` | `dronhltumzkngwwktesf` | `db.dronhltumzkngwwktesf.supabase.co` | `SUPABASE_URL` (all 6 services), `SUPABASE_ANON_KEY` (frontend) |

Each project has its own distinct database host and its own distinct API keys — no credential is shared across environments. `SUPABASE_URL`/`SUPABASE_ANON_KEY` were set directly (not sensitive enough to require Mac's manual involvement — the anon key is a client-safe key by design). **`SUPABASE_SERVICE_ROLE_KEY` is still outstanding for all three environments** — Supabase's MCP tooling deliberately does not expose service role keys (same principle as any other high-sensitivity credential in this project), so Mac needs to retrieve each project's service role key from its own dashboard (Project Settings → API → `service_role` secret) and provide it once per environment. Once provided, it gets set on `api-gateway`, `ai-orchestrator`, `sports-intel-layer`, and `workers` for that environment only — never reused across environments.

## Adding a new secret

1. Mac generates it on the provider's dashboard.
2. Claude Code (or Mac directly) sets it as a Railway environment variable on the specific service(s) and environment(s) that need it — never all services by default, matching least-privilege.
3. If the secret is needed at CI/CD deploy time rather than runtime, it goes into GitHub Actions environment secrets instead (or in addition).
4. Update the inventory table above in the same change.

## Rollback note

Rotating or revoking a secret is not the same operation as a code rollback (Volume 2 §9's incident protocol). If a live incident is credential-related (a leaked key), revoke/rotate the credential first, then decide separately whether a code rollback is also needed.
